<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/rabbit
        http://www.springframework.org/schema/rabbit/spring-rabbit-1.5.xsd
        http://www.springframework.org/schema/task
        http://www.springframework.org/schema/task/spring-task.xsd">

    <bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer" />

    <task:executor id="serviceTaskExecuter" pool-size="5"/>

    <rabbit:connection-factory
            id="serviceConnectionFactory"
            host="${spring.rabbitmq.host}"
            port="${spring.rabbitmq.port}"
            username="guest"
            password="guest"/>

    <rabbit:admin id="serviceAdmin" connection-factory="serviceConnectionFactory" />

    <bean id="ActivityRabbitListener" class="sabbat.location.infrastructure.service.adapter.ActivityRabbitListener" scope="singleton">
        <constructor-arg ref="jsonDtoParser" />
        <constructor-arg ref="activityApplicationService" />
    </bean>

    <bean id="ActivityRabbitListenerTyped" class="sabbat.location.infrastructure.service.adapter.ActivityMessageRabbitListener" scope="singleton" />


    <rabbit:queue id="activityCommandQueue"
                  name="${location.activity.queue.command}"
                  declared-by="serviceAdmin"
                  auto-declare="true"
                  durable="true" />

    <rabbit:topic-exchange
            id="activityCommandExchange"
            name="${location.activity.exchange.command}"
            declared-by="serviceAdmin"
            auto-declare="true">
        <rabbit:bindings>
            <rabbit:binding queue="activityCommandQueue" pattern="${location.activity.routingkey.command.all}"/>
                </rabbit:bindings>
    </rabbit:topic-exchange>

    <bean id="jackson2JsonMessageConverter" class="org.springframework.amqp.support.converter.Jackson2JsonMessageConverter" />

<!--   <rabbit:listener-container task-executor="serviceTaskExecuter"
                               connection-factory="serviceConnectionFactory"
                              acknowledge="auto"
                              concurrency="3"
                              max-concurrency="10"
                               auto-startup="false"
                              message-converter="jackson2JsonMessageConverter">

       <rabbit:listener ref="ActivityRabbitListener"
                        queue-names="${location.activity.queue.command}"
                        method="onMessage"/>
&lt;!&ndash;       <rabbit:listener ref="ActivityRabbitListenerTyped"
                        queue-names="${location.activity.queue.command}"
                        method="onMessage"/>&ndash;&gt;
    </rabbit:listener-container>-->

    <bean id="rabbitListenerContainerFactory"
          class="org.springframework.amqp.rabbit.config.SimpleRabbitListenerContainerFactory">
        <property name="connectionFactory" ref="serviceConnectionFactory"/>
        <property name="concurrentConsumers" value="3"/>
        <property name="maxConcurrentConsumers" value="10"/>
        <property name="taskExecutor" ref="serviceTaskExecuter" />
        <property name="messageConverter" ref="jackson2JsonMessageConverter" />
    </bean>

</beans>